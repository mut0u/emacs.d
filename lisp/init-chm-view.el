(require 'chm-view)




(setq chm-view-delay 1.0);; the default time delay is too shot that the process has not finish so have to enlage it.

(setq browse-url-browser-function 'w3m-browse-url)


(provide 'init-chm-view)
